import { DateFormatForJobPipe } from './date-format-for-job.pipe';

describe('DateFormatForJobPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatForJobPipe();
    expect(pipe).toBeTruthy();
  });
});
