import { DateFormatWihtoutStringPipe } from './date-format-wihtout-string.pipe';

describe('DateFormatWihtoutStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatWihtoutStringPipe();
    expect(pipe).toBeTruthy();
  });
});
